---
interface Props {
  href: string;
  variant?: 'primary' | 'secondary' | 'ghost';
  label: string;
  target?: string;
}
const { href, variant = 'primary', label, target } = Astro.props;
---
<a class:list={["btn", `btn--${variant}`]} href={href} target={target}>{label}</a>
