---
import SectionHeader from '../shared/SectionHeader.astro';
import { siteConfig } from '../../config/site.config';

const labels: Record<string, string> = {
  instagram: 'Instagram',
  facebook: 'Facebook',
  tiktok: 'TikTok',
  website: 'Sitio web',
  github: 'GitHub',
  linkedin: 'LinkedIn',
};

const links = Object.entries(siteConfig.social).filter(([, value]) => Boolean(value));
---
{links.length > 0 && (
  <section class="section" id="redes">
    <div class="container container--narrow">
      <SectionHeader
        eyebrow="Conecta"
        title="Sígueme y revisa mi trabajo"
        description="Más sobre lo que construyo y mi experiencia profesional."
        align="center"
      />
      <div class="social-list">
        {links.map(([name, url]) => (
          <a
            href={url as string}
            target="_blank"
            rel="noopener"
            class="social-pill"
            aria-label={labels[name] ?? name}
          >
            <span class="social-pill__icon" style={`--mask:url(/images/social/${name}.svg)`}></span>
            <span>{labels[name] ?? name}</span>
          </a>
        ))}
      </div>
    </div>
  </section>
)}
